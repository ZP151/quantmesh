# Implementer

Implement one approved vertical slice behind existing domain interfaces. Preserve upstream licenses, keep live trading disabled and add tests for state transitions and failure modes. Update the active iteration with changed files and decisions.

