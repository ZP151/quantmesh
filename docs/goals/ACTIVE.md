# Active Goal

- Status: active
- Objective: Deliver M4, a local Moomoo equity research and simulated-order reconciliation workflow
- Started: 2026-08-07
- Last checkpoint: 2026-08-08 (M4 issue #28 — Phase D, simulated execution reconciliation — committed on `feat/m4-moomoo-equity-workflow`; ADR-0006 recorded; 525 passed)
- Roadmap milestone: M2 (`DONE`) → M3 (`DONE`, merged `bcfb0b1`) → M4 (`ACTIVE`)
- Active iteration: `docs/iterations/0006-m4-moomoo-equity-workflow.md`
- GitHub issue: https://github.com/ZP151/quantmesh/issues/1 (closed 2026-08-07)
- Branch: `feat/m4-moomoo-equity-workflow` (solo fast lane; based on `origin/main`)
- Pull request: [merged #7](https://github.com/ZP151/quantmesh/pull/7) (slice #2), [merged #8](https://github.com/ZP151/quantmesh/pull/8) (slice #3), [merged #9](https://github.com/ZP151/quantmesh/pull/9) (slice #4), [merged #10](https://github.com/ZP151/quantmesh/pull/10) (slice #5), [merged #11](https://github.com/ZP151/quantmesh/pull/11) (slice #6), [merged #21](https://github.com/ZP151/quantmesh/pull/21) (M3 slice #1, issue #14), [merged #23](https://github.com/ZP151/quantmesh/pull/23) (final M3 PR, issues #15-#19, squash `bcfb0b1`)
- Agent environment checkpoint: `4565a70`

## Completed

- Repository and GitHub remote established.
- Codex/Claude collaboration environments and Agent Skills installed.
- Roadmap, domain context, ADRs and iteration ledger established.

## Current frontier

1. Split issue #1 into single-session vertical tickets with explicit blocking edges. — done 2026-08-07, tickets #2-#6
2. [#2](https://github.com/ZP151/quantmesh/issues/2) order lifecycle — implemented, reviewed, verified, committed, **merged to main via PR #7 (squash `f68682c`) 2026-08-07**. Issue #2 auto-closed on merge.
3. [#3](https://github.com/ZP151/quantmesh/issues/3) deterministic matcher — implemented, reviewed, verified, **merged to main via PR #8 (squash `ba01eda`) 2026-08-07**. Issue #3 auto-closed on merge.
4. [#4](https://github.com/ZP151/quantmesh/issues/4) portfolio accounting with fees and risk limits — implemented, reviewed, verified, committed `a331188`, **merged to main via PR #9 (squash `62e2397`) 2026-08-07**. Issue #4 auto-closed on merge.
5. [#5](https://github.com/ZP151/quantmesh/issues/5) SQLite event persistence, replay and reconciliation — implemented, reviewed, verified, committed `60415a1`, **merged to main via PR #10 (squash `349eb40`) 2026-08-07**. Issue #5 auto-closed on merge.
6. [#6](https://github.com/ZP151/quantmesh/issues/6) paper account API observability — implemented, reviewed, verified, committed `6920cc0`, **merged to main via PR #11 (squash `25ca09d`) 2026-08-07**. Issue #6 auto-closed on merge. **M2 slice sequence complete and merged.**
7. Merge gate — **COMPLETE 2026-08-07**: chain merged to main in order as squash `f68682c`, `ba01eda`, `62e2397`, `349eb40`, `25ca09d`; each remote feature branch deleted after its merge; issues #2-#6 closed on merge, issue #1 closed with evidence; main verified (110 passed, ruff clean) and CI green on main pushes.
8. **Next: M3** — data foundation and experiment registry (provider registry, normalized bar/order-book/event schemas, Parquet/DuckDB lake, dataset manifests, experiment registry). Plan: `docs/iterations/0005-m3-data-foundation.md`; issues #17-#19 are executed on `feat/m3-data-foundation` under the solo fast lane: one tested/reviewed commit and checkpoint per issue, then one final M3 PR. M3 slice #1 (issue #14) merged to main as squash `0bee38f` 2026-08-07; issue #14 closed. **Slice #2 (issue #15, Parquet/DuckDB lake + ADR-0003) committed 2026-08-07** — 34 lake tests, adversarial review fixed symbol/interval path validation, SQL escaping, naive-bound fail-closed, atomic temp+rename shard writes; ADR-0003 accepted (pyarrow deliberately not added; pandas is the deterministic write bridge). **Slice #3 (issue #16, dataset manifests) committed 2026-08-07** — `DatasetManifest`/`SeriesCoverage` models, `ManifestWriter` (coverage scan, revision bump, unique temp files), `Lake.dataset()` freshness gate; review fixed symlink escape, stray-file crashes, Windows reserved names, None/Unicode raw exceptions, Dataset constructor check; 47 manifest+layout tests. **Slice #4 (issue #17, provider registry) committed 2026-08-07** — `Provider` ABC + `ProviderMode` (fixture/sandbox/live), `ProviderRegistry` keyed by `Venue` refusing SANDBOX/LIVE (fixture-only per AGENTS.md), `FixtureProvider` base with injectable fixture dir, hyperliquid + moomoo fixture adapters, 6 fixture files, provider-isolation exit-criterion test; adversarial review fixed venue/symbol fail-closed gaps (moomoo books/trades, cross-venue instruments), naive-timestamp UTC drift, unknown-side silent SELL, mixed-interval fixtures, empty-fixture fail-open, raw KeyError/ValidationError leaks, wheel package-data; 23 provider tests. **Slice #5 (issue #18, experiment registry) committed 2026-08-07** — `Experiment` (deterministic setup-hash ID from dataset+revision+commit+parameters; metrics are results, not identity), `ExperimentRegistry` (JSONL under `settings.experiments_dir`, atomic temp+replace appends, git-HEAD commit default, pin validated through the lake gate at record AND resolve), clean-checkout integration test proving the first M3 exit criterion; adversarial review fixed NaN/Infinity parameters bricking the registry via id/serialization divergence (rejected at the model boundary), duplicate IDs passing read validation (read-time detection with line attribution), dup-check ordering, root-is-a-file fail-closed; 22 experiment tests. **Slice #6 (issue #19, scheduled ingestion + gap detection) committed 2026-08-07** — `IngestionJob`/`Ingestor` (fetch window re-opens the UTC day of the last stored tick so the day shard is rebuilt wholesale from the provider; full-OHLCV no-op comparison; stale-manifest healing) and `coverage_gaps` (`SeriesGap`/`CoverageReport`: vanished series, missing day shards via interval-grid expected days, undeclared bytes); adversarial review fixed 2 real bugs (content-blind no-op check silently dropping last-day corrections/retractions; write-without-manifest failure permanently gating the dataset until the next run heals it) plus duckdb-exception wrapping in `scan_series`/`read_bars`, and empty-dataset fail-closed; 8 regression tests, 288 total passing; roundtrip test completes the second M3 exit criterion (missing observations detected by lake quality and coverage report). **Round-2 final-review fixes (amended into the slice #6 commit 2026-08-07)**: monotonic-coverage invariant in `ManifestWriter` — refuses vanished series, shrunk rows, moved-back end (unless `rewritten`), moved-forward start always (closes the interior-day-loss masking bug); manifest removal is the documented override; `Lake.read_bars` fails closed on NULL timestamps; `_reject_symlinks` on the raw read/write surface; `coverage_gaps` blind spot documented; 9 regression tests, 297 total passing. **Final M3 PR #23 opened 2026-08-07** — CI caught two packaging/correctness gaps before green: `duckdb` promoted from the research extra to core dependencies (the lake hard-imports it and CI installs `.[dev]`), and `_reject_symlinks` now checks the terminal path itself (a symlinked symbol directory or shard file passed through; only Linux CI runs the symlink tests — Windows skips them). **Squash-merged to main as `bcfb0b1` 2026-08-07; issues #15-#19 closed; branch deleted. M3 is DONE — all four exit criteria met.** Note: issue numbers shifted twice — the M2 completion-records PR took #12 and the squash-divergence tracking issue took #13.
9. **M4 active** — implementation detail, phases, reusable components, external gate, and delivery protocol live in `docs/iterations/0006-m4-moomoo-equity-workflow.md`; issues #25-#28 (Phases A-D, dependency-ordered: #26/#28 block on #25, #27 blocks on #26) executed on `feat/m4-moomoo-equity-workflow`; the sole human gate is the Phase E simulated-account OpenD drill. **Issue #25 (Phase A, OpenD contract and diagnostics) committed 2026-08-08** — `quantmesh.moomoo` fixture-first `MoomooOpenDClient` boundary with injected transport, typed errors (unavailable/auth-required/sdk-missing/protocol), `OpenDCapabilities` probe with the locked-session rule, OpenD settings (env `QUANTMESH_MOOMOO_*`), `quantmesh-moomoo probe` operator command (redacted, writes nothing, reads no credentials); ADR-0004 recorded; 26 tests, 321 total passing; review fixed auth-as-state reporting and classifier unit coverage. **Issue #26 (Phase B, market-data adapter) committed 2026-08-08** — pandas-free wire payload contract derived from the vendored SDK source (klines 3-tuple on every path; venue-local wall-clock strings; `AuType` keys), `SdkTransport` market-data methods, `MoomooDataAdapter` (payload→Bar/TradeEvent/Quote, fail-closed, venue-local→UTC via zoneinfo/tzdata, tzdata declared core dependency), `MoomooOpenDProvider` (explicit-construction-only LIVE, registry refusal tested, venue-local date bounds + UTC range filter, interval/autype echo cross-checks), fixture data through the full M3 lake path (US/HK, daily/5m); ADR-0004 extended (decisions 8-13); 92 Moomoo tests, 389 total passing; review fixed the lake round-trip assertion surface (metadata is request-side, ADR-0003), a ternary-precedence bug in the currency check, and provider fail-closed on non-mapping payloads. **Issue #27 (Phase C, research baselines) committed 2026-08-08** — `quantmesh.research` walk-forward report stack per ADR-0005: `StrategyReport`/`WalkForwardSpec`/`CostModel`/`UniverseMember`/`WindowResult` (count-based non-overlapping windows, one shared cost model charged once per window on one-way turnover, fixed metrics schema with None-not-fabricated sharpe/annualized), deterministic 16-hex `report_id` (setup-only hash over sorted universe), `ReportRegistry` (JSONL, atomic appends, fail-closed reads with line attribution, pin validated through the lake manifest gate), pure no-RNG baselines (momentum top half / mean-reversion bottom half / risk-parity inverse vol), `run_walk_forward` + `run_baseline_report` (misaligned series, mixed lengths, empty members, cross-venue symbol collisions all fail closed), byte-reproducible artifacts with `created_at` excluded; ADR-0005 recorded; 55 Phase C tests, 443 total passing; review fixed the cross-venue symbol-overwrite hazard found in adversarial audit. **Issue #28 (Phase D, simulated execution reconciliation) committed 2026-08-08** — `OrderEvent`/`Fill(broker_fill_id, fee)` domain events with `OrderStateMachine` (ACCEPTED/CANCELED/REJECTED-with-reason adoptable), `OrderJournal` JSONL single source of truth for the broker_order_id↔order_id mapping (atomic appends, fail-closed reads, history-preserving snapshot replace), `SimulatedFixtureTransport` (deterministic JSONL phase script; B-N ids; `lost_acks` withhold acks for the disconnect the remark channel recovers), wire models with venue-local→UTC by market prefix failing closed at the boundary, `MoomooExecutionAdapter` explicit-construction-only with `TrdEnv.SIMULATE` pinned and REAL refused before the wire, reconciliation service (explicit `BROKER_STATUS_TO_DOMAIN`, recovered mappings WARNING non-blocking, broker-ahead = pending, matched = terminal agreement on both sides; adoption only for matched/pending clean pairs — divergence/missing/ambiguous/fee-less refused; idempotent), `quantmesh-moomoo paper-order`/`reconcile` CLI (report-only by default, --apply explicit, --at replay, tolerance flags; both Phase E-gated exit 3 without --fixture; lost-ack order recorded unacknowledged); ADR-0006 recorded (6 decisions); smoke drill converges lost-ack → remark recovery → adoption → matched with 0 findings; 87 new tests, 525 total passing. Next: Phase E (operator gate) — record the human gate (local OpenD + Moomoo simulated-account drill) in iteration 0006, then final M4 PR.

## Last verification

```text
pytest -q: 525 passed, 3 skipped (symlink creation not permitted) on feat/m4-moomoo-equity-workflow after the issue #28 commit, 2026-08-08
ruff check src tests: passed
git diff --check: passed
git submodule status: clean
GitHub Actions: green on main (last verified on the M3 merge bcfb0b1); pending on the issue #26/#27/#28 checkpoint pushes
```

## Blockers

None.

## Resume instructions

Run `/goal`. Read the active M4 iteration for implementation detail, split its fixture-first phases into issues, then continue from the first unblocked ticket. Do not enable external or live execution.
