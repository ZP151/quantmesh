# Active Goal

- Status: active
- Objective: Deliver M7, the unified research and portfolio engine (M4 Phase E gate and M5 operator drill still pending; M5's final PR awaits the drill; M6's final PR #38 stacks behind it, M7's final PR stacks behind that)
- Started: 2026-08-07
- Last checkpoint: 2026-08-08 (M7-1 / issue #39 Phase A committed and pushed — feature and model registries: `quantmesh.research.features` (`FeatureSpec`/`FeatureSet`/`FeatureRegistry`, five bar builtins validated at record AND compute, `compute_features` opening datasets once per pin through the lake manifest gate, `frame_digest`), `quantmesh.research.models` (`MODEL_TYPES=("linear",)` — pure-numpy `LinearModel` codec with canonical `quantmesh-linear-v1` JSON bytes; `ModelSpec`/`ModelRecord`/`ModelRegistry` with byte-addressed artifacts, sha256 re-verified on load, record append last so a crash leaves at worst an unreferenced orphan), settings `features_dir`/`models_dir`, ADR-0009 recorded (registry discipline + the research-extra/CI `.[dev,research]` dependency contract), 60 new tests with cross-root acceptance drills (identical frames via `frame_digest`, byte-identical model artifacts), full suite 1041 passed / 3 skipped, ruff/diff/submodules clean; iteration 0009 acceptance criterion 1 checked off)
- Roadmap milestone: M2 (`DONE`) → M3 (`DONE`, merged `bcfb0b1`) → M4 (`ACTIVE`, gate recorded) → M5 (`ACTIVE`, drill gate recorded) → M6 (`ACTIVE`, PR #38 open) → M7 (`ACTIVE`)
- M4 final PR: deferred until a human runs the Phase E drill (local OpenD + Moomoo simulated account) — exact operator steps in iteration 0006
- Active iteration: `docs/iterations/0009-m7-unified-research-and-portfolio-engine.md`
- GitHub issue: https://github.com/ZP151/quantmesh/issues/1 (closed 2026-08-07)
- Branch: `feat/m7-unified-research-and-portfolio-engine` (solo fast lane; branched from the M6 tip `7c4a0d1`, stacks until the M6 PR merges)
- Pull request: [merged #7](https://github.com/ZP151/quantmesh/pull/7) (slice #2), [merged #8](https://github.com/ZP151/quantmesh/pull/8) (slice #3), [merged #9](https://github.com/ZP151/quantmesh/pull/9) (slice #4), [merged #10](https://github.com/ZP151/quantmesh/pull/10) (slice #5), [merged #11](https://github.com/ZP151/quantmesh/pull/11) (slice #6), [merged #21](https://github.com/ZP151/quantmesh/pull/21) (M3 slice #1, issue #14), [merged #23](https://github.com/ZP151/quantmesh/pull/23) (final M3 PR, issues #15-#19, squash `bcfb0b1`), [open #38](https://github.com/ZP151/quantmesh/pull/38) (final M6 PR, issues #34-#37 — stacked on the M5 branch, waits for the M5 PR/human drill gate); M7 final PR to be opened on `feat/m7-unified-research-and-portfolio-engine` (issues #39-#43, stacks behind #38)
- Agent environment checkpoint: `4565a70`

## Completed

- Repository and GitHub remote established.
- Codex/Claude collaboration environments and Agent Skills installed.
- Roadmap, domain context, ADRs and iteration ledger established.

## Current frontier

1. Split issue #1 into single-session vertical tickets with explicit blocking edges. — done 2026-08-07, tickets #2-#6
2. [#2](https://github.com/ZP151/quantmesh/issues/2) order lifecycle — implemented, reviewed, verified, committed, **merged to main via PR #7 (squash `f68682c`) 2026-08-07**. Issue #2 auto-closed on merge.
3. [#3](https://github.com/ZP151/quantmesh/issues/3) deterministic matcher — implemented, reviewed, verified, **merged to main via PR #8 (squash `ba01eda`) 2026-08-07**. Issue #3 auto-closed on merge.
4. [#4](https://github.com/ZP151/quantmesh/issues/4) portfolio accounting with fees and risk limits — implemented, reviewed, verified, committed `a331188`, **merged to main via PR #9 (squash `62e2397`) 2026-08-07**. Issue #4 auto-closed on merge.
5. [#5](https://github.com/ZP151/quantmesh/issues/5) SQLite event persistence, replay and reconciliation — implemented, reviewed, verified, committed `60415a1`, **merged to main via PR #10 (squash `349eb40`) 2026-08-07**. Issue #5 auto-closed on merge.
6. [#6](https://github.com/ZP151/quantmesh/issues/6) paper account API observability — implemented, reviewed, verified, committed `6920cc0`, **merged to main via PR #11 (squash `25ca09d`) 2026-08-07**. Issue #6 auto-closed on merge. **M2 slice sequence complete and merged.**
7. Merge gate — **COMPLETE 2026-08-07**: chain merged to main in order as squash `f68682c`, `ba01eda`, `62e2397`, `349eb40`, `25ca09d`; each remote feature branch deleted after its merge; issues #2-#6 closed on merge, issue #1 closed with evidence; main verified (110 passed, ruff clean) and CI green on main pushes.
8. **Next: M3** — data foundation and experiment registry (provider registry, normalized bar/order-book/event schemas, Parquet/DuckDB lake, dataset manifests, experiment registry). Plan: `docs/iterations/0005-m3-data-foundation.md`; issues #17-#19 are executed on `feat/m3-data-foundation` under the solo fast lane: one tested/reviewed commit and checkpoint per issue, then one final M3 PR. M3 slice #1 (issue #14) merged to main as squash `0bee38f` 2026-08-07; issue #14 closed. **Slice #2 (issue #15, Parquet/DuckDB lake + ADR-0003) committed 2026-08-07** — 34 lake tests, adversarial review fixed symbol/interval path validation, SQL escaping, naive-bound fail-closed, atomic temp+rename shard writes; ADR-0003 accepted (pyarrow deliberately not added; pandas is the deterministic write bridge). **Slice #3 (issue #16, dataset manifests) committed 2026-08-07** — `DatasetManifest`/`SeriesCoverage` models, `ManifestWriter` (coverage scan, revision bump, unique temp files), `Lake.dataset()` freshness gate; review fixed symlink escape, stray-file crashes, Windows reserved names, None/Unicode raw exceptions, Dataset constructor check; 47 manifest+layout tests. **Slice #4 (issue #17, provider registry) committed 2026-08-07** — `Provider` ABC + `ProviderMode` (fixture/sandbox/live), `ProviderRegistry` keyed by `Venue` refusing SANDBOX/LIVE (fixture-only per AGENTS.md), `FixtureProvider` base with injectable fixture dir, hyperliquid + moomoo fixture adapters, 6 fixture files, provider-isolation exit-criterion test; adversarial review fixed venue/symbol fail-closed gaps (moomoo books/trades, cross-venue instruments), naive-timestamp UTC drift, unknown-side silent SELL, mixed-interval fixtures, empty-fixture fail-open, raw KeyError/ValidationError leaks, wheel package-data; 23 provider tests. **Slice #5 (issue #18, experiment registry) committed 2026-08-07** — `Experiment` (deterministic setup-hash ID from dataset+revision+commit+parameters; metrics are results, not identity), `ExperimentRegistry` (JSONL under `settings.experiments_dir`, atomic temp+replace appends, git-HEAD commit default, pin validated through the lake gate at record AND resolve), clean-checkout integration test proving the first M3 exit criterion; adversarial review fixed NaN/Infinity parameters bricking the registry via id/serialization divergence (rejected at the model boundary), duplicate IDs passing read validation (read-time detection with line attribution), dup-check ordering, root-is-a-file fail-closed; 22 experiment tests. **Slice #6 (issue #19, scheduled ingestion + gap detection) committed 2026-08-07** — `IngestionJob`/`Ingestor` (fetch window re-opens the UTC day of the last stored tick so the day shard is rebuilt wholesale from the provider; full-OHLCV no-op comparison; stale-manifest healing) and `coverage_gaps` (`SeriesGap`/`CoverageReport`: vanished series, missing day shards via interval-grid expected days, undeclared bytes); adversarial review fixed 2 real bugs (content-blind no-op check silently dropping last-day corrections/retractions; write-without-manifest failure permanently gating the dataset until the next run heals it) plus duckdb-exception wrapping in `scan_series`/`read_bars`, and empty-dataset fail-closed; 8 regression tests, 288 total passing; roundtrip test completes the second M3 exit criterion (missing observations detected by lake quality and coverage report). **Round-2 final-review fixes (amended into the slice #6 commit 2026-08-07)**: monotonic-coverage invariant in `ManifestWriter` — refuses vanished series, shrunk rows, moved-back end (unless `rewritten`), moved-forward start always (closes the interior-day-loss masking bug); manifest removal is the documented override; `Lake.read_bars` fails closed on NULL timestamps; `_reject_symlinks` on the raw read/write surface; `coverage_gaps` blind spot documented; 9 regression tests, 297 total passing. **Final M3 PR #23 opened 2026-08-07** — CI caught two packaging/correctness gaps before green: `duckdb` promoted from the research extra to core dependencies (the lake hard-imports it and CI installs `.[dev]`), and `_reject_symlinks` now checks the terminal path itself (a symlinked symbol directory or shard file passed through; only Linux CI runs the symlink tests — Windows skips them). **Squash-merged to main as `bcfb0b1` 2026-08-07; issues #15-#19 closed; branch deleted. M3 is DONE — all four exit criteria met.** Note: issue numbers shifted twice — the M2 completion-records PR took #12 and the squash-divergence tracking issue took #13.
9. **M4 active** — implementation detail, phases, reusable components, external gate, and delivery protocol live in `docs/iterations/0006-m4-moomoo-equity-workflow.md`; issues #25-#28 (Phases A-D, dependency-ordered: #26/#28 block on #25, #27 blocks on #26) executed on `feat/m4-moomoo-equity-workflow`; the sole human gate is the Phase E simulated-account OpenD drill. **Issue #25 (Phase A, OpenD contract and diagnostics) committed 2026-08-08** — `quantmesh.moomoo` fixture-first `MoomooOpenDClient` boundary with injected transport, typed errors (unavailable/auth-required/sdk-missing/protocol), `OpenDCapabilities` probe with the locked-session rule, OpenD settings (env `QUANTMESH_MOOMOO_*`), `quantmesh-moomoo probe` operator command (redacted, writes nothing, reads no credentials); ADR-0004 recorded; 26 tests, 321 total passing; review fixed auth-as-state reporting and classifier unit coverage. **Issue #26 (Phase B, market-data adapter) committed 2026-08-08** — pandas-free wire payload contract derived from the vendored SDK source (klines 3-tuple on every path; venue-local wall-clock strings; `AuType` keys), `SdkTransport` market-data methods, `MoomooDataAdapter` (payload→Bar/TradeEvent/Quote, fail-closed, venue-local→UTC via zoneinfo/tzdata, tzdata declared core dependency), `MoomooOpenDProvider` (explicit-construction-only LIVE, registry refusal tested, venue-local date bounds + UTC range filter, interval/autype echo cross-checks), fixture data through the full M3 lake path (US/HK, daily/5m); ADR-0004 extended (decisions 8-13); 92 Moomoo tests, 389 total passing; review fixed the lake round-trip assertion surface (metadata is request-side, ADR-0003), a ternary-precedence bug in the currency check, and provider fail-closed on non-mapping payloads. **Issue #27 (Phase C, research baselines) committed 2026-08-08** — `quantmesh.research` walk-forward report stack per ADR-0005: `StrategyReport`/`WalkForwardSpec`/`CostModel`/`UniverseMember`/`WindowResult` (count-based non-overlapping windows, one shared cost model charged once per window on one-way turnover, fixed metrics schema with None-not-fabricated sharpe/annualized), deterministic 16-hex `report_id` (setup-only hash over sorted universe), `ReportRegistry` (JSONL, atomic appends, fail-closed reads with line attribution, pin validated through the lake manifest gate), pure no-RNG baselines (momentum top half / mean-reversion bottom half / risk-parity inverse vol), `run_walk_forward` + `run_baseline_report` (misaligned series, mixed lengths, empty members, cross-venue symbol collisions all fail closed), byte-reproducible artifacts with `created_at` excluded; ADR-0005 recorded; 55 Phase C tests, 443 total passing; review fixed the cross-venue symbol-overwrite hazard found in adversarial audit. **Issue #28 (Phase D, simulated execution reconciliation) committed 2026-08-08** — `OrderEvent`/`Fill(broker_fill_id, fee)` domain events with `OrderStateMachine` (ACCEPTED/CANCELED/REJECTED-with-reason adoptable), `OrderJournal` JSONL single source of truth for the broker_order_id↔order_id mapping (atomic appends, fail-closed reads, history-preserving snapshot replace), `SimulatedFixtureTransport` (deterministic JSONL phase script; B-N ids; `lost_acks` withhold acks for the disconnect the remark channel recovers), wire models with venue-local→UTC by market prefix failing closed at the boundary, `MoomooExecutionAdapter` explicit-construction-only with `TrdEnv.SIMULATE` pinned and REAL refused before the wire, reconciliation service (explicit `BROKER_STATUS_TO_DOMAIN`, recovered mappings WARNING non-blocking, broker-ahead = pending, matched = terminal agreement on both sides; adoption only for matched/pending clean pairs — divergence/missing/ambiguous/fee-less refused; idempotent), `quantmesh-moomoo paper-order`/`reconcile` CLI (report-only by default, --apply explicit, --at replay, tolerance flags; both Phase E-gated exit 3 without --fixture; lost-ack order recorded unacknowledged); ADR-0006 recorded (6 decisions); smoke drill converges lost-ack → remark recovery → adoption → matched with 0 findings; 87 new tests, 525 total passing. **Phase E gate recorded and deferred 2026-08-08** — the sole external-state gate (human local OpenD + Moomoo simulated-account drill, exact operator steps in iteration 0006) is pending; all safe M4 work is complete and checked off, and the final M4 PR stays closed until a human runs the drill per the delivery protocol (operator-validation evidence required). Branch checkpoint `b6fd2ea` pushed (5fbeb6c + gate record).
10. **M5 active** — Hyperliquid testnet workflow; plan in `docs/iterations/0007-m5-hyperliquid-testnet-workflow.md`; issues #29-#33 (Phases A-E, dependency-ordered: #30/#31/#32 block on #29, #31/#33 block on #30) executed on `feat/m5-hyperliquid-testnet-workflow` (branched from the M4 tip — M5 reuses the M4 journal/state-machine/reconciliation core and stacks until M4 merges). The sole human gate is the Phase E testnet wallet drill. Security posture: testnet-only surface, mainnet refused before the wire, keys in memory only via injected signer/env var, no real-money path. Reuse: pinned hyperliquid-python-sdk submodule; Hummingbot connectors reference-only. **Issue #29 (Phase A, REST/WS market data with reconnect and gap recovery) committed 2026-08-08** — `quantmesh.hyperliquid` package: typed errors; `wire` parsers with SDK-source contracts (candles/l2Book/trades/allMids/funding/meta/spotMeta), fail-closed on shape violations; `SdkRestTransport` (lazy, import-guarded, testnet pinned, mainnet refused at construction, SDK exceptions → typed unavailable) + `ScriptedRestTransport` drill stub; WS = deterministic `StreamSupervisor` state machine + scripted `SimulatedStreamTransport` + `HyperliquidStream` asyncio pump (exponential backoff; the SDK's `WebsocketManager` has no reconnect — ADR-0007): subscribe-on-open, per-channel last-data tracking, ping every 50 s anchored at the connection instant, disconnect → dark findings, reconnect → resubscribe + REST re-sync (candles merged over the gap, book replaced by a fresh snapshot — full level arrays, no deltas —, trades reported as typed gap findings); a clean socket close is a disconnect too (no reconnect storm); `HyperliquidFixtureProvider` (wire-shaped fixtures through the real parsers, registry-registerable) / `HyperliquidLiveProvider` (explicit-construction-only, bounded ranges mandatory, trades fail closed); 6 wire fixtures; settings (`QUANTMESH_HYPERLIQUID_*`); ADR-0007 recorded (5 decisions + Phase B/C/E extension hooks). 75 new tests. Review caught two real defects before commit: the live pump `async with`'d a raw coroutine (TypeError on Python 3.13 — now awaited before entering the context manager; clean closes back off) and `parse_trades_frame` envelope/data contract mismatch (aligned to the data payload). Acceptance criterion 1 checked off; 600 passed, 3 skipped; ruff/diff/submodules clean; checkpoint pushed. **Issue #30 (Phase B, testnet execution adapters + reconciliation) committed 2026-08-08** — ADR-0006 contract types extracted to `quantmesh.execution.reconciliation` (Moomoo binding re-exports; shared vocabulary, venue-local engines); `quantmesh.hyperliquid.exchange`: wire models, signer boundary (`InMemorySigner` exactly 32 bytes, `signer_from_env` fail-closed), `ExchangeTransport` boundary, `ScriptedExchangeTransport` (JSONL phases, deterministic oids, `lost_acks`), `SdkExchangeTransport` (lazy + import-guarded, testnet pinned, mainnet refused at construction; market orders refuse reduce_only — the pinned SDK's `market_open` hard-codes it — closing goes through reduce-only LIMIT orders), `HyperliquidExecutionAdapter` (journal-first client ids — recorded before the wire, reused ids refuse submission — per-order ack errors → REJECTED(reason), filled acks → ACCEPTED only, cancel by oid or cloid, lost ack leaves PENDING unacknowledged); fail-closed parsers (`tid`/`hash` fill identity, cloid echo mismatch, top-level err, 34-char cloid shape); `quantmesh.hyperliquid.reconciliation`: cloid channel replaces the remark channel (recovered mapping = MAPPING/WARNING non-blocking, oid re-stamped at adoption, ambiguous channels divergent), derived statuses for a surface with no order-status endpoint (open = remaining size tolerates journal partial fills; inactive fills-only rows → FILLED by fill total / journal terminal status / else CANCELED), ack-terminal unclaimed orders matched (note only when no venue order id was ever received), M4-discipline compares (quantity/price/fees/fill identity/positions, declared tolerances; fee-less fills MISSING_DATA; revoked stamped fills), adoption only for matched/pending clean pairs (fee-less and overfill refusals, idempotent, derived terminal events stamped with venue evidence); 6-phase drill fixture `wire_exchange_script.jsonl`; ADR-0007 Phase B extension recorded; acceptance criterion 2 checked off; 86 new tests — the acceptance drill converges to {matched: 2, pending: 0, missing: 0, divergent: 0} with 0 findings / 0 refusals; review caught and fixed four real defects (BrokerFill missing cloid for the snapshot merge, parse_fill rejecting hash-identity rows, `_call` swallowing kwargs, a 33-char fixture cloid caught by the venue's own shape check). 686 passed, 3 skipped; ruff/diff/submodules clean; checkpoint pushed. **Issue #31 (Phase C, risk pre-submission surface) committed 2026-08-08** — `quantmesh.hyperliquid.risk`: pure `evaluate_order` gate with four fail-closed checks (stale-data window — missing/future/over-age book timestamps refuse; reduce-only posture; leverage bound on the resulting signed position vs equity — full closes skip, missing equity/entry fail closed; liquidation-distance floor — venue `liquidationPx` scaled to the size-weighted entry of the resulting position, funding-corrected conservatively, measured against the l2Book mid, already-at-or-beyond a refusal, reductions/full closes skip, direction flips rebase to the new entry); typed `RiskKind`/`RiskRefusal`/`RiskDecision` (allowed only with zero refusals); `RiskContextProvider` Protocol; `FundingLedger` fee-like journal entry (`funding.jsonl`, signed deltas vs each coin's running cumulative — a three-record compounding defect caught and fixed in review — atomic writes, fail-closed reads with line attribution); adapter wiring — `risk_limits` + `risk_context` paired at construction or ValueError, gate runs BEFORE the journal-first recording, a refusal raises `HyperliquidRiskRefusalError` and consumes nothing (empty journal, zero wire calls in the drill); ADR-0007 Phase C extension recorded; acceptance criterion 3 checked off; 40 new tests (26 gate, 7 adapter wiring, 7 funding ledger); 726 passed, 3 skipped; ruff/diff/submodules clean; checkpoint pushed. **Issue #32 (Phase D, order-book imbalance and volatility baselines) committed 2026-08-08** — `quantmesh.hyperliquid.signal`: pure depth-weighted `book_imbalance` ((Σbid−Σask)/(Σbid+Σask) over the full book; empty books fail closed, one-sided books are ±1.0) and `imbalance_by_bar` (snapshots bucketed into [timestamp, timestamp+interval) windows, per-bar means aligned 1:1 with the bar grid; outside-every-window, no-snapshot, non-monotonic, symbol-mismatch, mixed-interval, and empty inputs all fail closed); `quantmesh.research.baselines` — `low_volatility_weights` (bottom half by train realized vol, zero-vol included, sorted tie-break) and `book_imbalance_weights` (top half by train mean signal) as `run_walk_forward` strategy branches; `signals_by_symbol` validated against the bar grid (universe + length), weights computed from each window's train slice only (no-lookahead proven by test); signal digest (sha256 over sorted canonical JSON) folds into `report_id` and is pinned as `signals_digest` on `StrategyReport` itself (recorded setup and identity cannot disagree; None keeps the legacy id); `STRATEGIES` extended with `low_volatility`/`book_imbalance` rather than opened up; exports through the hyperliquid and research surfaces; ADR-0007 Phase D extension recorded; acceptance criterion 4 checked off; 55 new/updated tests (16 signal, 39 baseline) — end-to-end signal reports byte-reproduce across registry roots and different signal inputs yield different report ids; review caught the identity/pin mismatch before commit (the report model recomputed the expected id without the digest, rejecting every signal-driven report) and corrected a wrong tie-break test expectation; 755 passed, 3 skipped; ruff/diff/submodules clean; checkpoint pushed. **Issue #33 (Phase E, wallet isolation + operator drill gate) committed 2026-08-08** — two leak vectors fixed in `exchange.py`: the default dataclass repr of `InMemorySigner` printed key bytes into logs/exceptions/dumps (now redacted `<InMemorySigner redacted (32 bytes)>`), and `signer_from_env` echoed the malformed env value in its error message (a secret being parsed is itself key material — the message now states the shape without repeating the value); `SdkExchangeTransport(None)` fails closed at construction (no default-key path); `tests/test_hyperliquid_wallet_isolation.py` — the secret-handling suite: signer repr redaction; env-parse error redaction; construction-without-key fail-closed; a full scripted order/cancel/reconcile drill (lost ack → cloid recovery → cancel → fills + positions → clean) with a real 32-byte key, after which every durable surface — journal JSONL, the shipped drill script, captured DEBUG logs, and the whole scratch tree — is scanned for the key hex, key-bytes repr, and signer repr, and the wired risk-refusal path is scanned too (typed refusal message clean, empty journal, zero wire calls); ADR-0007 Phase E extension recorded; acceptance criterion 5 checked off; 5 new tests; 760 passed, 3 skipped; ruff/diff/submodules clean; checkpoint pushed. **M5 operator gate recorded and deferred 2026-08-08** — the sole external-state gate for M5: a human funds a Hyperliquid *testnet* wallet, exports the key via `QUANTMESH_HYPERLIQUID_PRIVATE_KEY` (session only, history off), runs the health + read-only market-data checks (`SdkRestTransport`/`HyperliquidLiveProvider`, no key), then a deliberately small order/cancel/reconcile drill (0.001 BTC, ACCEPTED → CANCELED, reconciliation clean), and records redacted evidence; exact steps are in iteration 0007 Phase E. All safe M5 work is complete and checked off; the final M5 PR stays closed until a human runs the drill.
11. **M6 active** — prediction-market intelligence; plan in `docs/iterations/0008-m6-prediction-market-intelligence.md`; issues #34-#37 (Phases A-D) executed on `feat/m6-prediction-market-intelligence` (branched from the M5 tip `f1a4b84`, stacks until the M5 PR merges). No human gate exists in M6: every surface is public read-only market data with no credentials and no order path. **Issue #34 (Phase A, canonical event models + Polymarket adapters) committed 2026-08-08** — `quantmesh.events` venue-neutral models (`EventVenue`/`ResolutionRule` with NFKC-normalized 64-hex fingerprint revalidation/`EventMarket`/`Outcome`/`MarketQuote`/`ImpliedProbability`, aware-timestamp and bounds validators); `quantmesh.polymarket` (typed errors; fail-closed wire parsers pinned to the vendored `py-clob-client-v2` source and wire shapes recorded live 2026-08-08 — live `/book` omits the SDK's last_trade_price/min_order_size/neg_risk/tick_size (read from the market object), level order worst-first (best levels extracted order-agnostically), `/prices-history` object rows, `/fee-rate` → `{"base_fee": bps}` (SDK's `or 0` fail-open fallback not replicated), errors `{"error": str}`, retired markets 404; `SdkPolyTransport` keyless — `ClobClient(host, chain_id, key=None)` proven by a faked-import test; `PolyMarketDataAdapter` pure mapping; `PolyFixtureProvider` registry-registered / `PolyLiveProvider` explicit-construction-only; no bar and no trades surface on the pinned contract → `fetch_bars`/`fetch_trades` fail closed, `polymarket_trades.json` deliberately omitted from the canonical set); 10 wire fixtures + 2 canonical M3-shaped fixtures derived through the real parsers; `QUANTMESH_POLYMARKET_*` settings; ADR-0008 recorded (5 decisions + Phase B/C/D extension hooks); 64 new tests; review fixed the per-market `_rule_text` cross-assignment bug and an unused setting; 822 passed, 3 skipped; ruff/diff/submodules clean; checkpoint pushed. **Issue #35 (Phase B, Kalshi market-data adapter) committed 2026-08-08** — `quantmesh.kalshi` (typed errors; wire models and fail-closed parsers pinned to shapes recorded live 2026-08-08 — the docs were stale on every probed surface: trades live at `/markets/trades?ticker=` and candlesticks at `/series/{series}/markets/{ticker}/candlesticks` (the docs' routes 404), `/markets` rejects the `status=active` filter (recorded 400), both orderbook ladders ascend worst-first resting bids with YES asks derived as 1 − NO bid per the docs' own rule and verified exact (0.42/0.69, sizes 6.00/4.00) against the market object at the same instant, candlesticks report `volume_fp` → genuine bar surface with zero-volume rows (only `price.previous_dollars`) producing no bar and bar-open re-based from `end_period_ts` − period, resolution inline on settled/finalized markets (`result` + `settlement_ts`), error shapes `{"error": {code, message}}` and `{"msg": ...}` plus the plain-text "API has been moved" 401 from the migration host, `/series/{ticker}` identity in `ticker`; `HttpxKalshiTransport` httpx-based against the settings-pinned base URL with the migration host refused at construction (M5 testnet-pin parity); `KalshiMarketDataAdapter` pure wire→domain mapping; `KalshiFixtureProvider` registry-registered / `KalshiLiveProvider` explicit-construction-only with bounded-range discipline and the market→event→series chain for bars; no auth surface anywhere); 16 wire fixtures + 4 canonical `kalshi_{events,markets,books,trades}.json` fixtures derived through the real parsers; `QUANTMESH_KALSHI_*` settings; ADR-0008 Phase B extension recorded; 57 new tests; the tests exposed two real fail-closed gaps in the parsers (`parse_market` leaked raw KeyErrors, now wrapped like `parse_markets`; `parse_series` read the docs' `series_ticker` field the wire doesn't carry — identity is in `ticker`) plus 10 E501 over-long lines from the earlier session, all fixed; 881 passed, 3 skipped; ruff/diff/submodules clean; checkpoint pushed. Acceptance criterion 1 fully checked off. **Issue #36 (Phase C, implied probabilities + calibration + forecast reports) committed 2026-08-08** — `quantmesh.events.calibration`: pure fee/spread/liquidity-aware transforms with documented formulas — break-even-derived fee-aware mid (`mid + fee_rate * half_spread`, from the shifted no-arbitrage interval `[b(1-f), a(1+f)]`; zero for Kalshi whose `taker_fee_bps = 0` — its quadratic fee is absorbed by the confidence band, never fabricated), liquidity confidence (depth score saturating at 2000 total contracts × spread score 1.0 at ≤ 2 ticks with a 0.2 floor at 10 ticks, one-sided halved, rounded 4 dp), history fallback blending the quote toward the recent price mean below 0.5 confidence and never diluting at or above it, Brier pair/mean/bin (half-open bins, `p == 1.0` in the last, empty bins `None`) and liquidity-weighted Brier; `quantmesh.events.forecast`: the M5 report-stack discipline applied to calibration — setup-only 16-hex id over commit + sorted universe (composite `venue:venue_market_id` keys) + window spec + bins; JSONL registry with atomic appends, fail-closed reads with line attribution, duplicate-id refusal, no lake pin (the recorded universe is the setup); byte-stable artifacts `report.json` (created_at excluded)/`windows.csv`/`calibration.csv` across registry roots — with point-in-time replay enforced in `_outcome_value`: an observation strictly older than `resolved_at` never sees the outcome (one exactly on the instant does), split resolutions refused (binary Brier needs a binary resolution), resolution without `resolved_at` fails closed, unresolved windows report None; `events/__init__.py` exports the full surface; 69 new tests — the calibration suite pinned the documented arithmetic and the forecast suite proved replay by flipping a resolution after window close (closed windows stay None under both outcomes; overlapping windows flip 0.184967 → 0.324967) plus byte-reproducibility across registry roots; review-by-test exposed the missing `count` default on CalibrationBin rows; 951 passed, 3 skipped; ruff/diff/submodules clean; checkpoint pushed. Acceptance criteria 2-4 checked off. **Issue #37 (Phase D, cross-platform event mapping + point-in-time replay acceptance) committed 2026-08-08** — `quantmesh.events.mapping`: reconciliation-disciplined event pairing (ADR-0006 discipline applied to events) — four independent evidence kinds (TITLE: normalized-title equality via NFKC/casefold/whitespace-collapse; OUTCOME_SET: sorted normalized outcome-name sets; EXPIRY: both present within the 3600s tolerance; RESOLUTION_RULE: fingerprint equality), status a deterministic function of the evidence (≥2 → MATCHED, exactly 1 → PENDING, ≥2 strong candidates on one side → AMBIGUOUS with all evidence recorded, no candidate → unmatched, never guessed), order-invariant 16-hex `pair_key` (sha256 over the sorted member ids), `EventPairing`/`EventMappingReport` consistency validators (evidence sortedness, pending/matched evidence counts, pair-key recompute, paired-vs-unmatched disjointness), fail-closed inputs (empty universes, duplicate venue ids), and the append-only `MappingLedger` JSONL at `~/.quantmesh/mappings/` (atomic temp+replace appends, fail-closed reads with line attribution, duplicate refusal by (pair_key, status, evidence signature), changed evidence appends as history — a PENDING pair that later matches upgrades to MATCHED, `by_pair` returns the history in order); `events/__init__.py` exports the full mapping surface; settings `mappings_dir`. 30 new tests — the acceptance drill pins the honest fixture verdict: every fixture market is binary Yes/No, so all 18 candidate pairs carry exactly the outcome-set evidence and stay PENDING, including the known-real Fed correspondence (Sept 2026 Polymarket questions vs the Apr 2027 Kalshi question differ in title/expiry/rule — the algorithm refuses to claim it), while a drill-only synthetic pair (DRILL- ids, distinct title/outcomes/expiry/rule) matches on all four evidence kinds, resolves on the positive side at T+12h over a 20-hour observation grid, and feeds `run_forecast_report` end-to-end: unresolved Fed market (quote 0.42/0.69 → implied 0.555 at 0.2 confidence) reports None windows, drill windows close before resolution report None, later windows evaluate p = 0.5 against outcome 1.0 (brier 0.25; resolved counts 0/3/5 pin the point-in-time boundary — the observation exactly on the resolution instant sees the outcome), reliability bins land in bin 5, artifacts byte-stable across registry roots; probes against the fixture reality corrected two planned assumptions (the kalshi Fed event id is `KXFED-27APR-T3.50`, and the zero-volume candles fixture fabricates no bars so the drill uses the recorded quote directly — the history fallback remains unit-tested); 981 passed, 3 skipped; ruff/diff clean; checkpoint pushed. Acceptance criterion 5 checked off — M6 acceptance criteria 1-5 complete. **Final M6 PR #38 opened 2026-08-08** (base = `feat/m5-hyperliquid-testnet-workflow`, the M5 remote tip is exactly the M6 base `f1a4b84` so the PR shows the 5 M6 commits cleanly) with the full acceptance evidence; M6 has no human gate, so the only blocker to squash-merge + issue closes #34-#37 is the stacked M5 PR, which awaits the M5 operator testnet drill.
12. **M7 active** — unified research and portfolio engine; plan in `docs/iterations/0009-m7-unified-research-and-portfolio-engine.md`; issues #39-#43 (Phases A-E, dependency-ordered: #40/#41/#42 block on #39, #41/#42 block on #40, #43 blocks on #41) executed on `feat/m7-unified-research-and-portfolio-engine` (branched from the M6 tip `7c4a0d1`, stacks until the M6 PR #38 merges). No human gate exists in M7: every surface is local computation over pinned fixtures — no credentials, no new execution path (the M2 paper kernel remains the only execution surface; promotion/kill-switch wiring is report-only until M10). Reuse: lightgbm (>=4.5,<5) + scikit-learn (>=1.5,<2) already pinned in the research extra, new pins scipy/hmmlearn/arch plus the CI `.[dev,research]` install change (documented extension of the dependency contract) in Phase B; M3 experiment-registry discipline → feature/model registries; M5 report stack + `STRATEGIES` branches → pipeline reports; M5 signal-digest discipline → feature-set digests; M6 Brier/reliability discipline → ensemble calibration; M5 risk checks + M6 implied probabilities → portfolio constraints; M2 kernel replay → scenario tests. **Issue #39 (Phase A, feature and model registries) committed and pushed 2026-08-08** — `quantmesh.research.features`: `FeatureKind` (bar-only; orderbook/event kinds documented extensions, refused at construction and compute — never recordable-but-uncomputable), five bar builtins (momentum/log_return/rolling_mean/rolling_std/realized_vol) with parameter contracts validated at record AND compute, setup-only 16-hex `feature_id` (commit + name + kind + venue + symbol + interval + dataset pins, never results), `FeatureSet` with sorted member ids (order never changes identity; canonical model feature order = sorted member ids), `compute_features` (datasets opened once per (dataset, revision) pin through the lake manifest gate; fail-closed on missing pins, duplicate names, non-finite closes, out-of-order/duplicate timestamps, empty frames), `frame_digest` (sorted canonical JSON, UTC ISO timestamps, repr floats — the Phase E drift reference); `quantmesh.research.models`: `MODEL_TYPES=("linear",)` — the pure-numpy `LinearModel` codec (`np.linalg.lstsq`, canonical `quantmesh-linear-v1` JSON with exact float reprs → byte-stable across registry roots), setup-only `model_id` (hyperparameters + featureset digest + dataset pins + train-window bounds), `ModelSpec`/`ModelRecord` (metrics and artifact sha256 are results, never identity; created_at normalized to UTC), `ModelRegistry` (JSONL with atomic temp+replace appends, fail-closed reads with line attribution, duplicate refusal, pin validated through the lake manifest gate before any write — the registry directory is absent after a refused record; artifact written before the record append; `load` re-verifies the sha256 — tampered/missing artifacts fail closed); settings `features_dir`/`models_dir`; ADR-0009 recorded (6 decisions: setup-only identity, pin-before-write, byte-addressed artifacts, the linear codec, kinds-as-extensions, the research-extra/CI dependency contract). 60 new tests (29 feature + 31 model): identity sensitivity across all setup fields, builtin arithmetic pinned to closed forms, all fail-closed reads, tamper/missing artifact refusals, and the acceptance drills — two pinned lakes + two registries reproduce identical feature frames (`frame_digest` equality) and byte-identical model artifacts (sha256/id/metrics equal across roots; the artifact loaded from one root predicts exactly on the other's frame). Review-by-test caught six real defects in the test surface itself before commit: a non-hex commit fixture rejected by the pin pattern, a helper that clobbered explicit wrong ids, `window=` overrides not folded into `parameters`, closed-form comparisons including the NaN warm-up prefix, a duplicate-timestamps premise impossible through `write_bars` (day-shard replacement — duplicates only arise within one call, by design reported via quality), and `.loc[list]` frame alignment (reindex onto the feature index instead). Full suite 1041 passed, 3 skipped; ruff/diff/submodules clean; checkpoint pushed. Acceptance criterion 1 checked off. **Next: issue #40 Phase B (baseline pipelines)** — pyproject research extra gains scipy/hmmlearn/arch pins, CI installs `.[dev,research]`, venv installs the research stack, then the lazy import-guarded LightGBM/logistic/HMM/GARCH pipelines as `run_walk_forward` strategy branches.

## Last verification

```text
pytest -q: 1041 passed, 3 skipped (symlink creation not permitted) on feat/m7-unified-research-and-portfolio-engine after the issue #39 (Phase A) commit, 2026-08-08
ruff check src tests: passed
git diff --check: passed
git submodule status: clean
GitHub Actions: green on main (last verified on the M3 merge bcfb0b1); pending on the M4/M5/M6/M7 checkpoint pushes
```

## Blockers

None.

## Resume instructions

Run `/goal`. Read the active M5 iteration for implementation detail, split its fixture-first phases into issues, then continue from the first unblocked ticket. Do not enable external or live execution.
