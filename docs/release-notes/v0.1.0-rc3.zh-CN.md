# QuantMesh v0.1.0-rc3 发布说明（中文）

包元数据 `0.1.0rc3`（PEP 440），两个来源一致并被测试固定；标签
`v0.1.0-rc3` 打在通过验证的合并提交上。v0.1.0-rc2 已于 2026-08-09
被操作员拒绝：其标签声称 `v0.1.0-rc2`，包实际仍报告 `0.1.0rc1`，
且既有版本测试因自身固定 `0.1.0rc1` 而无法发现漂移。rc2 标签保留
作为历史记录不做改写；本 RC 修复缺陷并新增本应拦住它的门禁。

## 相对 rc2 的变更

- **三处版本元数据全部修复**：`pyproject.toml`、
  `src/quantmesh/__init__.py` 与 `tests/test_release.py` 的固定值
  现一致为 `0.1.0rc3`（工作站页脚、导入结果与 `pip show` 一致）。
- **新增发布门禁步骤**（`tools/check_release_version.py`，克隆后
  第 3 步）：断言 `pyproject.toml` 与 `__init__` 一致；最新的
  `docs/release-notes/v0.1.0-rc*.md` 声明同一版本（rc2 的漂移——
  说明写 rc2、元数据是 rc1——现在会直接使门禁失败）；HEAD 上任何
  版本标签与包版本一致。标签与包版本不匹配从此无法再通过门禁。
- 其余与 rc2 相同：React 19 + Vite + Tailwind 4 + shadcn/ui SPA
  （ADR-0013）、带来源标识与两次点击重置的确定性 `--demo` 运行
  时、完整的研究到纸面交易浏览器闭环、免密公共数据源与标注
  synthetic 的降级、CSV/JSON/Parquet 导入（逐行拒绝原因）、键盘/
  WCAG 2.2 AA 覆盖与 Impeccable 视觉审查通过。

## 发布验证

干净检出发布门禁（`python tools/release_gate.py`）是发布证明，
本次 RC 的结论：

```text
RELEASE GATE PASSED — the release is verified in a fresh
deterministic environment.
```

计数：**1865 通过，0 失败，0 跳过**（Python 全套，含共享浏览器
缓存下的 Playwright E2E）；**18/18** 前端单元测试；**5/5** SPA
Playwright E2E；黄金路径 53/53；pip-audit 未发现已知漏洞；克隆
检出保持干净。各步骤耗时见下文门禁汇总。

## 安装

要求：Python 3.13 与 git。

```text
git clone <本仓库>
cd QuantMesh
python -m venv .venv
.venv\Scripts\activate        # Windows；POSIX 用 source .venv/bin/activate
python -m pip install -e ".[dev,research,e2e]"
```

## 启动（演示模式，推荐验收方式）

```text
quantmesh-workstation --demo
```

然后打开 <http://127.0.0.1:8765/app/>。工作站只绑定回环地址，
运行确定性种子纸面会话。每个页面带来源标识；页头重置按钮恢复
初始演示根目录。

## 启动（操作员模式）

```text
quantmesh-workstation
```

打开同一 SPA 但基于操作员状态根目录（纸面账户初始为空）。演示与
导入数据都带标识并与非演示操作员状态隔离；重置只清演示根目录。

## 停止 / 重置

- 停止：在工作站终端按 `Ctrl+C`。
- 演示根目录：页头重置按钮，或删除 `--demo-root` 目录。
- 全部生成状态：删除数据根目录 `~/.quantmesh/`（可用 `QUANTMESH_*`
  环境变量改路径，见 `src/quantmesh/settings.py`）。

## 安全控制（全部默认拒绝，与 rc1 一致）

- 实盘交易默认关闭（`allow_live_trading: false`）；任何真实资金
  交易、主网钱包签名、真实经纪商订单、凭据使用、付费基础设施、
  AI 下单权限，都必须先获得明确的人工批准，并在启用台账中记录
  携带逐字门禁文本的启用迁移。
- 工作站仅绑定回环；模型网关默认回环端点，拒绝远程 URL（除非
  显式以 `allow_remote=True` 构造）。
- 全局与分市场急停开关在会计风险门中强制生效，并由 E2E 演练
  验证。
- Connectors 探针只读；免密公共数据路径不携带任何凭据（也不存在
  凭据）。

## 已知限制

- 本 RC 唯一执行面是内部纸面 venue；Moomoo 与 Hyperliquid 适配器
  仅为只读行情面。
- 免密公共路径只覆盖演示标的的近期快照（Hyperliquid）；SDK 缺失
  时降级为标注 synthetic 的 fixture 行，而不是失败。
- 导入数据集归属当前数据根，演示重置会清掉（重置=恢复初始种子根
  目录）。
- 共享 Playwright 浏览器缓存存在时 E2E 用例运行，否则报告为
  skipped。

## 门禁汇总

发布分支 `0014-rc3-version-gate` @ 打标签提交（2026-08-09）执行
`python tools/release_gate.py`；起始源码检出干净，成功后删除临时
根目录：

```text
RELEASE GATE PASSED — the release is verified in a fresh
deterministic environment.
```

## 外部门禁

Moomoo OpenD 模拟账户与 Hyperliquid 测试网演练仍为可选、依赖操作
员的验证。它们不阻塞本 RC，也不授权主网/实盘操作。
