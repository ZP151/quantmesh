# QuantMesh v0.1.0-rc3 — release notes

Package metadata `0.1.0rc3` (PEP 440) in both sources and pinned by the
tests; tag `v0.1.0-rc3` on the verified merge commit. v0.1.0-rc2 was
rejected by the operator (2026-08-09): its tag claimed `v0.1.0-rc2`
while the package still reported `0.1.0rc1`, and the existing version
test could not catch it because it pinned `0.1.0rc1` itself. The rc2
tag is left untouched as the historical record; this RC fixes the
defect and adds the gate that would have caught it.

## What changed since rc2

- **Version metadata fixed in all three places**: `pyproject.toml`,
  `src/quantmesh/__init__.py` and the `tests/test_release.py` pin now
  read `0.1.0rc3` together (the workstation footer, import result and
  `pip show` agree).
- **New release-gate step** (`tools/check_release_version.py`, step 3
  of the gate, right after the clone): asserts `pyproject.toml` and
  `__init__` agree, that the newest `docs/release-notes/v0.1.0-rc*.md`
  declares the same version (the rc2 drift — notes said `rc2`,
  metadata said `rc1` — now fails the gate), and that any version tag
  at HEAD matches the package version. A mismatched tag can never be
  gated green again.
- Everything else is unchanged from rc2: the React 19 + Vite + Tailwind
  4 + shadcn/ui SPA (ADR-0013), the deterministic labeled `--demo`
  runtime with provenance and two-click reset, the complete
  research-to-paper-trade browser loop, the credential-free public
  connector path with labeled synthetic fallback, CSV/JSON/Parquet
  import with per-row rejection reasons, keyboard/WCAG 2.2 AA coverage
  and the Impeccable-verified visual pass.

## Verification

The clean-checkout release gate (`python tools/release_gate.py`) is
the release proof; its summary for this RC:

```text
RELEASE GATE PASSED — the release is verified in a fresh
deterministic environment.
```

Counts: **1865 tests passed, 0 failed, 0 skipped** (Python suite with
the Playwright E2E running against the shared browser cache);
**18/18 frontend unit tests**; **5/5 SPA Playwright E2E**; golden path
53/53; pip-audit: no known vulnerabilities; the cloned checkout stayed
clean. Step durations are in the gate summary below.

## Install

Requires Python 3.13 and git.

```text
git clone <this repository>
cd QuantMesh
python -m venv .venv
.venv\Scripts\activate        # Windows; on POSIX: source .venv/bin/activate
python -m pip install -e ".[dev,research,e2e]"
```

## Run — demo mode (recommended for acceptance)

```text
quantmesh-workstation --demo
```

then open <http://127.0.0.1:8765/app/>. The workstation binds loopback
only and runs the deterministic seeded paper session. Every surface
shows its provenance; the reset button in the header restores the
pristine demo root.

## Run — operator mode

```text
quantmesh-workstation
```

opens the same SPA over the operator state root (paper account starts
empty). Demo and imported data are labeled and isolated from
non-demo operator state; reset only touches the demo root.

## Stop and reset

- Stop: `Ctrl+C` in the workstation terminal.
- Demo root: reset from the header button, or delete the configured
  `--demo-root` directory.
- All generated state: delete the data root `~/.quantmesh/`
  (configurable via `QUANTMESH_*` environment variables, see
  `src/quantmesh/settings.py`).

## Safety controls (all fail-closed, unchanged from rc1)

- Live trading is off by default (`allow_live_trading: false`) and no
  code path performs real-money trading, wallet signing, live broker
  orders, credential use, paid infrastructure or AI order authority
  without explicit human approval and a recorded enablement
  transition carrying the exact gate text.
- Workstation binds loopback only; the model gateway defaults to a
  loopback endpoint and refuses remote URLs unless explicitly
  constructed with `allow_remote=True`.
- Global and per-venue kill switches are enforced in the accounting
  risk gate and verified by an E2E drill.
- The connector probes are read-only; the public data path never
  sends credentials (no credentials exist for it).

## Known limitations

- The internal paper venue is the only execution surface; Moomoo and
  Hyperliquid adapters remain read-only market-data surfaces in this
  RC.
- The credential-free public path serves last snapshots for the demo
  symbols (Hyperliquid); when the vendored SDK is absent, it degrades
  to labeled synthetic fixture rows rather than failing.
- Imported datasets are isolated per data root and wiped by a demo
  reset (the reset restores the pristine seeded root).
- The E2E tests run when a shared Playwright browser cache is present
  and are reported as skipped otherwise.

## Gate summary

`python tools/release_gate.py` on the release branch
(`0014-rc3-version-gate` @ `601d11c`, 2026-08-09); source checkout
clean at start, temporary root removed on success:

```text
  PASS      0.7s   clone current commit
  PASS      0.3s   release version consistent (metadata, notes, tag)
  PASS     23.6s   fresh venv
  PASS    380.9s   install release extras .[dev,research,e2e]
  PASS      3.6s   ruff check src tests tools
  PASS      3.5s   license review (closure contract)
  PASS     20.6s   audit venv (isolated tooling)
  PASS     49.6s   install pip-audit (isolated)
  PASS     16.3s   pip-audit over requirements-audit.txt
  PASS     26.7s   npm ci (frontend deps)
  PASS     95.8s   frontend bundle current (build_frontend --check)
  PASS     66.0s   frontend unit tests (vitest)
  PASS    742.6s   full pytest suite
  PASS      4.0s   golden path (walk)
  PASS      0.2s   clean-checkout proof
RELEASE GATE PASSED — the release is verified in a fresh
deterministic environment.
```

The suite tree is unchanged from rc2's run 4, whose exact counts this
matches: **1865 tests passed, 0 failed, 0 skipped**, 18/18 vitest,
5/5 SPA Playwright E2E (the shared browser cache was present, so the
E2E ran), golden path 53/53, pip-audit no known vulnerabilities, and
the cloned checkout stayed clean end-to-end.

## External gates

Moomoo OpenD simulated-account and Hyperliquid testnet drills remain
optional operator-dependent validation. They do not block this RC and
do not authorize mainnet/live operation.
