# QuantMesh Claude Environment

`CLAUDE.md` is the Claude entrypoint and `AGENTS.md` is the canonical shared contract. Project-scoped skills are under `.claude/skills/`, and specialist role prompts are under `.claude/agents/`.

